<%- | Hash $team_members
| -%>

<!DOCTYPE=html>
<html>
<head>
    <title>The Team</title>
</head>
<body>
    <% $team_members.each | $key,$value | { -%>
        <h1><a href="./<%= $value["member_id"] %>.html"><%= $value["member_name"] %></h1>
    <% } -%>
</body>
